<div class="container p-4">
    <div class="jumbotron jumbo">
        <h1 class="display-4 text-center">Historial</h1>
        {{#if month}}
        <p class="lead text-center">Lista de horas extra por fecha {{year}}/{{month}}</p>
        {{else}}
        <p class="lead text-center">Lista de horas extra por fecha </p>
        {{/if}}
        <form action="/overTime/listRegisters" method="POST">
            <div class="form-group">
                <label for="">Seleccione una fecha</label>
                <input type="date" class="form-control" max="{{date.fecha}}" value="{{date.fecha}}" name="fecha"
                    placeholder="fecha" required>
            </div>
            <div class="form-group">
                <button class="btn btn-principal btn-block">Consultar</button>
            </div>
        </form>
    </div>
    <div class="row">
        <div class="col-md-12">
            <div class="card">
                <div class="card-title jumbo p-2">
                    <h4 class="text-center text-uppercase">Horas extra realizadas durante la el mes</h4>
                     {{#if dataGeneral}}
                     <p class="text-center"><a class ="btn btn-principal btn-central" href="/overTime/pdf/{{year}}/{{month}}"generar pdf>Generar pdf</a></p>
                     {{/if}}
                </div>
                <div class="card-body">
                    {{#if dataGeneral}}
                    <div class="table-responsive">
                        <table class="table table-striped table-sm">
                            <thead>
                                <tr>
                                    <th>Cédula</th>
                                    <th>Nombre</th>
                                    <th>Primer apellido</th>
                                    <th>Segundo apellido</th>
                                    <th>Fecha de realización</th>
                                    <th>Estado</th>
                                    <th>Cantidad</th>
                                    <th>Motivo</th>
                                    <th>Descargar pdf</th>
                                </tr>
                            </thead>
                            <tbody>
                                {{#each dataGeneral}}
                                <tr>
                                    <td>{{cedula}}</td>
                                    <td>{{nombre}}</td>
                                    <td>{{p_apellido}}</td>
                                    <td>{{s_apellido}}</td>
                                    <td>{{fecha}}</td>
                                    <td>{{informacion_estado}}</td>
                                    <td>{{cantidad_horas}}</td>
                                    <td>{{motivo}}</td>
                                     <td><a href="/overTime/employeepdf/{{id}}" class="btn btn-block btn-secondary"><i
                                                class="material-icons">
                                                get_app
                                            </i></td>
                                </tr>
                                {{/each}}
                            </tbody>
                        </table>
                    </div>
                    {{else}}
                    <p class=" text-center"> No hay registros</p>
                    {{/if}}
                </div>
            </div>
        </div>
    </div>
</div>